# --- INDIVIDUAL GROUPS ---

[ubuntu]
172.20.242.30  # Ubuntu Ecom
# 172.20.242.X  # Ubuntu Wks (REPLACE WITH DHCP IP)

[fedora]
172.20.242.40  # Fedora Webmail

[oracle]
172.20.242.20  # Splunk (Oracle Linux 9.2)

# --- OVERARCHING GROUP ---

[linux:children]
ubuntu
fedora
oracle

# --- CREDENTIALS & VARIABLES ---

[ubuntu:vars]
ansible_user=sysadmin
ansible_password=changeme
ansible_become_pass=changeme

[fedora:vars]
ansible_user=sysadmin
ansible_password=changeme
ansible_become_pass=changeme

[oracle:vars]
# Based on VM 3: Oracle uses root:changemenow
ansible_user=root
ansible_password=changemenow
ansible_become_pass=changemenow

[linux:vars]
# Connection stability for the first 15 seconds
ansible_connection=ssh
ansible_ssh_common_args='-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'
